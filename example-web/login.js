var input;

function setup() {
    createCanvas(windowWidth, windowHeight);
    input = createInput();
    input.size(width * 0.4, height * 0.1);
    input.position(width * 0.3, height * 0.3);
}

function draw() {
    
}